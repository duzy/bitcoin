project core

import (
    './src'
)

libs = $(src.xlibs)

build: src.archives src.objects src.programs
clean: src.clean

use:
	CXXFLAGS += -I"$//src"
	LDFLAGS += -L"$//src"
#	LDLIBS += "$//src/bitcoind.o"
	LDLIBS += $(libs)
